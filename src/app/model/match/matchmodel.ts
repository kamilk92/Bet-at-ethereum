export interface MatchModel {
  beginDate: Date;
  beginTime: number;
  ended: boolean;
  group: string;
  guest: string;
  guestScore: number;
  host: string;
  hostScore: number;
  id: number;
  points: number;
}